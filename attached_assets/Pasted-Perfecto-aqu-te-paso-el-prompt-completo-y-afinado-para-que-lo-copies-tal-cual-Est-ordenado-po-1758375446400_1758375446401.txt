Perfecto 🚀, aquí te paso el prompt completo y afinado para que lo copies tal cual. Está ordenado por prioridad, con checklists, contratos API y notas clave para evitar fallas:

⸻

📋 Prompt de Implementación – Qartha (VM / Replit)

Prioridades (en orden)
	1.	Autenticación real con tabla users + JWT en cookies HttpOnly.
	2.	Guardias de sesión en el SPA (login, redirect tras QR, fetch con credentials).
	3.	Roles (admin/visitor) en API y UI (eliminar ADMIN_TOKEN).
	4.	Edición completa de IDF (persistir diagrams, location, DFO, gallery, docs).
	5.	Alta completa de nuevos IDF (wizard/tabs con todos los campos).

⸻

⚙️ 0. Preparativos
	•	Si usas Postgres: renombra app/db/mongo.py → postgres.py.
	•	.env backend:

JWT_SECRET=change_me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60


	•	.env frontend:

VITE_API_BASE_URL=https://<tu-vm>/api



⸻

🔑 1. Autenticación JWT + tabla users

Tabla SQL

CREATE TABLE IF NOT EXISTS users(
  id SERIAL PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('admin','visitor')),
  full_name TEXT,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  last_login_at TIMESTAMPTZ
);

Archivos
	•	app/models/user_models.py: UserCreate, UserPublic, UserInDB, TokenPayload.
	•	app/core/security.py: hash_password(), verify_password(), create_access_token().
	•	app/core/config.py: añade JWT_*.
	•	app/routers/auth.py:
	•	POST /api/auth/login → set-cookie access_token (HttpOnly).
	•	POST /api/auth/logout → borra cookie.
	•	GET  /api/auth/me → devuelve UserPublic.
	•	app/main.py: incluye router y crea usuario admin inicial.

Checklist
	•	Tabla users creada.
	•	Login devuelve cookie.
	•	GET /me funciona.
	•	Usuario semilla admin creado.
	•	Eliminado ADMIN_TOKEN duro.

⸻

🔒 2. Guardias de sesión (SPA + QR)

Cambios
	•	Login.tsx: formulario email/contraseña → POST /api/auth/login con credentials:"include".
	•	AuthContext: guarda user, loading, login(), logout().
	•	api.ts: centraliza fetch con credentials:"include" y manejo de 401.
	•	ProtectedRoute: si no hay sesión → guarda redirect_to y manda a /login.
	•	Tras login → redirige a redirect_to o default cluster/proyecto.

Redirección QR
	1.	Al cargar app: guarda redirect_to en sessionStorage y en cookie.
	2.	Tras login: lee redirect_to y redirige allí.

Checklist
	•	Acceder a URL de QR sin login manda a /login.
	•	Tras login, redirige a la URL original del QR.
	•	Todas las llamadas fetch llevan credentials:"include".

⸻

👥 3. Roles admin / visitor

Backend
	•	get_current_admin y get_current_user.
	•	Sustituir verify_admin_token en admin_idfs.py, assets.py, devices.py.
	•	/public_idfs y /qr solo requieren get_current_user.

Frontend
	•	Ocultar CMS, AdminSidebar, botones “Add IDF” si role !== "admin".
	•	Mostrar mensaje/403 en pantallas restringidas.

Checklist
	•	Usuario visitor no puede entrar al CMS ni mutar datos.
	•	Usuario admin sí puede.
	•	Todos los endpoints con auth correcta.

⸻

🛠️ 4. Edición completa de IDF

SQL (Postgres)

ALTER TABLE idfs
  ADD COLUMN IF NOT EXISTS diagrams  JSONB NOT NULL DEFAULT '[]',
  ADD COLUMN IF NOT EXISTS location  JSONB NOT NULL DEFAULT '[]',
  ADD COLUMN IF NOT EXISTS dfo       JSONB,
  ADD COLUMN IF NOT EXISTS gallery   JSONB NOT NULL DEFAULT '[]',
  ADD COLUMN IF NOT EXISTS documents JSONB NOT NULL DEFAULT '[]';

Backend
	•	IdfUpsert con todos los campos (description, site, room, dfo, gallery, documents, diagrams, location).
	•	PUT /idfs/{code} acepta y devuelve todos los campos.

Frontend
	•	AdminSidebar: tabs → General / DFO / Gallery / Location / Diagrams / Documents.
	•	Uploads devuelven URL pública inmediata.

Checklist
	•	Campos gallery, docs, diagrams, location, dfo persisten y se leen.
	•	Ediciones reflejadas en UI sin F5.
	•	Borrado individual disponible.

⸻

🆕 5. Alta completa de IDF

Backend
	•	IdfCreate hereda de IdfUpsert + code.
	•	POST /idfs crea registro completo.

Frontend
	•	Reemplazar AddIdfDialog por wizard/tabs:
	1.	Básico (code, title, site, room)
	2.	Fiber/DFO
	3.	Location
	4.	Gallery
	5.	Diagrams
	6.	Documents
	•	Validar code (IDF-xxxx).
	•	Refrescar lista/detalle al crear.

Checklist
	•	Crear IDF con todos los campos.
	•	Nuevo IDF aparece en listado y detalle de inmediato.
	•	URL pública sigue siendo /cluster/proyecto/idf/{code}.

⸻

📡 Contratos API
	•	Auth
	•	POST /api/auth/login (cookie access_token)
	•	POST /api/auth/logout
	•	GET /api/auth/me
	•	IDFs
	•	GET /api/{cluster}/{project}/idfs
	•	GET /api/{cluster}/{project}/idfs/{code}
	•	POST /api/{cluster}/{project}/idfs (admin)
	•	PUT  /api/{cluster}/{project}/idfs/{code} (admin)
	•	Assets (admin)
	•	POST /api/{cluster}/{project}/assets/{code}/{asset_type}
	•	DELETE /api/{cluster}/{project}/assets/{code}/{asset_type}/{index}

⸻

✅ DoD Global
	•	QR abre → pide login si no hay sesión → tras login regresa a la URL original.
	•	Usuarios diferenciados por rol.
	•	IDFs editables con todos los campos.
	•	Alta de IDFs completa y visible de inmediato.
